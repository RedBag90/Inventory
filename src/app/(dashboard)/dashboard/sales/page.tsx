// Thin route — delegates entirely to the sales feature.
// No logic lives here. Import only from features/sales/index.ts.
export default function SalesRoute() {
  return null; // replace with <SalesPage /> from '@/features/sales'
}
