// Global shared types — only place types here that are used across 2+ features.
// Feature-specific types belong in features/<name>/types/.

export type Platform = 'KLEINANZEIGEN' | 'EBAY' | 'FACEBOOK' | 'OTHER';
export type ItemStatus = 'IN_STOCK' | 'SOLD';
