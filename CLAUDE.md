# CLAUDE.md

## gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools.

Available gstack skills:
- `/office-hours` — product interrogation, challenge assumptions before building
- `/autoplan` — generate an implementation plan from a design doc or conversation
- `/plan-ceo-review` — strategic challenge and scope review
- `/plan-eng-review` — engineering architecture and risk review
- `/plan-design-review` — design critique before implementation
- `/plan-devex-review` — developer experience review
- `/design-consultation` — explore design approaches with a specialist
- `/design-shotgun` — generate many design variants fast
- `/design-html` — produce an HTML/CSS mockup
- `/design-review` — review a design for polish and correctness
- `/review` — code review on the current branch
- `/ship` — full pre-ship checklist (review + QA + PR)
- `/land-and-deploy` — merge and deploy a PR
- `/canary` — canary deploy and monitor
- `/qa` — open a real browser and QA a staging URL
- `/qa-only` — browser QA without the review step
- `/cso` — security audit (OWASP + STRIDE)
- `/investigate` — root cause debugging methodology
- `/retro` — engineering retrospective
- `/document-release` — generate release notes
- `/learn` — deep-dive explanation of a concept or codebase area
- `/benchmark` — benchmark two implementations
- `/benchmark-models` — compare model outputs side by side
- `/browse` — web browsing skill (use this instead of MCP browser tools)
- `/scrape` — extract structured data from a URL
- `/context-save` — save current context for later restoration
- `/context-restore` — restore a previously saved context
- `/careful` — slow down and be extra careful on the next action
- `/freeze` — freeze a file or directory from AI edits
- `/guard` — add a guard comment to protect a section
- `/unfreeze` — remove a freeze
- `/gstack-upgrade` — upgrade gstack to the latest version
- `/devex-review` — developer experience audit
- `/skillify` — turn a workflow into a reusable skill
- `/sync-gbrain` — sync with GBrain knowledge base
- `/setup-deploy` — set up deployment configuration
- `/setup-gbrain` — set up GBrain integration
- `/setup-browser-cookies` — configure browser cookies for QA
- `/open-gstack-browser` — open the gstack browser tool
- `/make-pdf` — generate a PDF from content
- `/landing-report` — analyse a landing page
- `/codex` — run a task via OpenAI Codex CLI
- `/pair-agent` — spawn a pair-programming sub-agent
